{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "command": "cd '/c/Users/Professional/Desktop/vk-post-reposting-bot/vk-post-reposting-bot' && python -m py_compile $(echo '$FILE_PATH' | sed 's/^.*\\\\\\\\//' | grep -E '\\.py$' || echo '/dev/null')",
        "description": "Проверить синтаксис Python файла после редактирования"
      },
      {
        "matcher": "Write|Edit",
        "command": "cd '/c/Users/Professional/Desktop/vk-post-reposting-bot/vk-post-reposting-bot' && git add -A && git diff --cached --quiet || (git commit -m \"chore: auto-commit after edit [$(date +'%H:%M')]\" && git push)",
        "description": "Автокоммит + пуш в git после каждого редактирования"
      }
    ]
  },
  "autoAcceptPermissions": false,
  "allowedTools": [
    "Read",
    "Write",
    "Edit",
    "Glob",
    "Grep",
    "Bash"
  ]
}
